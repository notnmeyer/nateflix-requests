<h1>Editing <%= @request.title %></h1>

<%= render "form_admin", request: @request %>

<div>
  <%= link_to "Back to dashboard", root_url %> |
  <%= link_to "Delete", @request, data: { turbo_method: :delete } %>
</div>
