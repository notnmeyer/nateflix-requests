<!DOCTYPE html>
<html>
  <head>
    <title>Nateflix Requests</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%# <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous"> %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg text-bg-dark">
      <%= link_to "Nateflix Requests", root_url, class: "navbar-brand text-light" %>
      <ul class="navbar-nav ms-auto" id="nav">
        <% if signed_in? %>
            <li class="navbar-text text-light "><%= current_user.email %> (<%= admin_signed_in? ? "admin" : "user" %>)</li>
            <li class="nav-item"><%= link_to "logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link link-warning" %></li>
        <% else %>
          <li class="nav-item"><%= link_to "login", new_user_session_path, class: "nav-link link-light" %></li>
          <li class="nav-item"><%= link_to "admin", new_admin_session_path, class: "nav-link link-light" %></li>
        <% end %>
      </ul>
    </nav>

    <div id="messages" class="row">
      <div class="col-md-12">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
    </div>

    <%= yield %>
  </body>
</html>
