<!DOCTYPE html>
<html>
  <head>
    <title>Nateflix Requests</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%# <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous"> %>
  </head>

  <body class="bg-gradient-light">
    <nav class="navbar navbar-expand-lg text-bg-dark">
      <%= link_to "Nateflix Requests", root_url, class: "navbar-brand text-light" %>
      <ul class="navbar-nav ms-auto align-items-center" id="nav">
        <% if signed_in? %>
            <li class="navbar-text text-light"><%= pretty_email current_user.email %></li>
            <li class="nav-item">
              <%= button_to "Sign out", destroy_user_session_path, class: "btn btn-outline-warning btn-sm", method: :delete, data: { turbo: false } %>
            </li>
        <% end %>
      </ul>
    </nav>

    <div id="messages" class="row">
      <div class="col-md-12">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
    </div>

    <%= yield %>
  </body>
</html>
